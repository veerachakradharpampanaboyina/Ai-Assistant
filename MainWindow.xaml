<ui:FluentWindow x:Class="AIAssistant.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AIAssistant"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"
        Title="AI Assistant" Height="700" Width="900"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        Topmost="True">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:TitleBar Title="AI Assistant" Grid.Row="0" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10,5,10,5">
            <ComboBox x:Name="AiSelector" SelectionChanged="AiSelector_SelectionChanged" Width="120" Margin="0,0,10,0" SelectedIndex="0">
                <ComboBoxItem Content="ChatGPT"/>
                <ComboBoxItem Content="Gemini"/>
                <ComboBoxItem Content="Claude"/>
                <ComboBoxItem Content="Arena AI"/>
                <ComboBoxItem Content="DeepSeek"/>
                <ComboBoxItem Content="Z.ai"/>
            </ComboBox>
            <ui:Button Content="Auto-Fix Code" Click="AutoFix_Click" Margin="0,0,10,0"/>
            <ui:Button Content="Insert Response" Click="InsertResponse_Click" Appearance="Primary" Margin="0,0,10,0"/>
            <ui:Button Content="Screen Capture" Click="ScreenCapture_Click" Margin="0,0,10,0"/>
            <ui:Button Content="Settings" Click="OpenSettings_Click" Margin="0,0,10,0"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="10,0,10,5">
            <ui:Button Content="Set Workspace" Click="SetWorkspace_Click" Margin="0,0,10,0"/>
            <TextBlock x:Name="WorkspaceText" Text="No workspace..." VerticalAlignment="Center" Margin="0,0,10,0" Width="150" TextTrimming="CharacterEllipsis"/>
            <TextBlock Text="Canvas:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <ComboBox x:Name="CanvasSelector" Width="120" Margin="0,0,10,0" SelectedIndex="0" SelectionChanged="CanvasSelector_SelectionChanged">
                <ComboBoxItem Content="Code Project"/>
                <ComboBoxItem Content="Document"/>
                <ComboBoxItem Content="Presentation"/>
                <ComboBoxItem Content="Spreadsheet"/>
            </ComboBox>
            <TextBlock Text="Format:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <ComboBox x:Name="FormatSelector" Width="90" Margin="0,0,10,0" SelectedIndex="0">
                <ComboBoxItem Content="Any"/>
            </ComboBox>
        </StackPanel>

        <Grid Grid.Row="3" Margin="10,0,10,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <ui:TextBox x:Name="UserPromptText" Grid.Column="0" AcceptsReturn="True" TextWrapping="Wrap" Height="65" VerticalScrollBarVisibility="Auto" PlaceholderText="Enter a detailed task prompt, document data, presentation slides, or specific context here..."/>
            <StackPanel Grid.Column="1" Orientation="Vertical" Margin="10,0,0,0" VerticalAlignment="Center">
                <ui:Button Content="Init Agent" Click="InitAgent_Click" Appearance="Secondary" Margin="0,0,0,5" HorizontalAlignment="Stretch"/>
                <StackPanel Orientation="Horizontal">
                    <ToggleButton x:Name="AutoLoopToggle" Content="Auto-Loop" Checked="AutoLoopToggle_Checked" Unchecked="AutoLoopToggle_Unchecked" Margin="0,0,5,0"/>
                    <ui:Button Content="Force Exec" Click="ForceExecution_Click"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="5"/>
                <ColumnDefinition Width="350"/>
            </Grid.ColumnDefinitions>

            <wv2:WebView2 x:Name="webView" Source="https://chatgpt.com" Grid.Column="0" />

            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" Background="LightGray"/>

            <Grid Grid.Column="2" Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Text="Agent Execution Log" FontWeight="Bold" Margin="0,0,0,5" Grid.Row="0"/>
                <ListBox x:Name="AgentLogList" Grid.Row="1" ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" TextWrapping="Wrap" FontFamily="Consolas" FontSize="11"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <StackPanel x:Name="TerminalStatusPanel" Grid.Row="2" Orientation="Horizontal" Visibility="Collapsed" Margin="0,5,0,0">
                    <ui:ProgressRing IsIndeterminate="True" Width="16" Height="16" Margin="0,0,8,0"/>
                    <TextBlock x:Name="TerminalStatusText" Text="Running command..." VerticalAlignment="Center" FontStyle="Italic" TextTrimming="CharacterEllipsis"/>
                </StackPanel>
            </Grid>
        </Grid>

        <!-- Settings Overlay -->
        <Grid x:Name="SettingsOverlay" Grid.RowSpan="5" Visibility="Collapsed" Background="#80000000">
            <Border Background="{DynamicResource ApplicationBackgroundBrush}" CornerRadius="8" Padding="20" HorizontalAlignment="Center" VerticalAlignment="Center" Width="400">
                <StackPanel>
                    <TextBlock Text="Settings" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>
                    
                    <CheckBox x:Name="AllowScreenShareCheckbox" Content="Show this application in screen share" Checked="AllowScreenShareCheckbox_Changed" Unchecked="AllowScreenShareCheckbox_Changed" IsChecked="False"/>
                    <TextBlock Text="When unchecked, the app is hidden from screen recording/sharing." FontSize="11" Foreground="Gray" Margin="25,0,0,20" TextWrapping="Wrap"/>
                    
                    <ui:Button Content="Close" Click="CloseSettings_Click" HorizontalAlignment="Right" Appearance="Primary"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</ui:FluentWindow>
